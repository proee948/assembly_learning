.intel_syntax noprefix
.global main

.text
main:
xor rcx,rcx
xor rax,rax
lea rax,[rip + s1]
call _strlen
ret

_strlen:
str_loop:
mov bl,[rax]
cmp bl,0
je done
inc rcx
inc rax
jmp str_loop

done:
mov rax,rcx
ret

.data
s1: .string "123456789"
s2: .string "string2"
.bss
